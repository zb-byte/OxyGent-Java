# Git 使用指南

## 📦 Git仓库状态

Git仓库已初始化，所有代码已提交到本地仓库。

## 📊 当前状态

- ✅ Git仓库已创建
- ✅ 27个文件已跟踪
- ✅ 初始提交已完成
- ✅ `.gitignore` 已配置（忽略target等构建产物）

## 🔧 常用Git命令

### 查看状态
```bash
git status              # 查看工作区状态
git log                 # 查看提交历史
git log --oneline       # 简洁的提交历史
```

### 提交更改
```bash
git add .               # 添加所有更改
git add <file>           # 添加指定文件
git commit -m "消息"     # 提交更改
```

### 查看差异
```bash
git diff                # 查看未暂存的更改
git diff --cached       # 查看已暂存的更改
git diff HEAD           # 查看与上次提交的差异
```

### 分支管理
```bash
git branch              # 查看所有分支
git branch <name>        # 创建新分支
git checkout <name>      # 切换到分支
git checkout -b <name>   # 创建并切换到新分支
```

### 远程仓库（可选）
如果需要关联远程仓库：
```bash
git remote add origin <url>    # 添加远程仓库
git push -u origin main         # 推送到远程
git pull                        # 从远程拉取
```

## 📋 提交规范建议

建议使用清晰的提交信息格式：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**类型（type）**：
- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建/工具相关

**示例**：
```bash
git commit -m "feat(devops): 添加新的部署智能体"
git commit -m "fix(framework): 修复ReActAgent的工具调用路由问题"
git commit -m "docs: 更新业务开发指南"
```

## 🔍 查看被忽略的文件

查看哪些文件被 `.gitignore` 忽略：
```bash
git status --ignored
```

## 📁 当前Git配置的文件

- ✅ 所有源代码文件（`.java`）
- ✅ 配置文件（`pom.xml`, `application.properties`）
- ✅ 文档文件（`.md`）
- ✅ `.gitignore`

## ❌ 被忽略的文件

- `target/` - Maven构建产物
- `.idea/`, `.vscode/` - IDE配置文件
- `*.class` - 编译后的类文件
- `.DS_Store` - Mac系统文件

## 🚀 下一步

1. **继续开发**：正常编写代码，使用 `git add` 和 `git commit` 提交
2. **创建分支**：为新功能创建分支 `git checkout -b feature/new-feature`
3. **关联远程**：如果需要，可以关联GitHub/GitLab等远程仓库

