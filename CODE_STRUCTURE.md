# 代码结构说明

## 🎯 设计原则

本项目将代码分为**框架代码**和**业务代码**两部分，便于：
1. **框架代码复用**：其他业务可以直接使用框架
2. **业务代码隔离**：每个业务模块独立，互不干扰
3. **清晰的职责划分**：框架开发者 vs 业务开发者

---

## 📂 目录结构

```
src/main/java/
│
├── framework/                    # ⭐ 框架代码（可复用基础设施）
│   │
│   ├── agent/                    # 智能体框架
│   │   ├── Agent.java           # 智能体接口
│   │   ├── AgentFramework.java  # 智能体管理框架
│   │   └── ReActAgent.java      # ReAct智能体实现
│   │
│   ├── llm/                      # LLM客户端框架
│   │   ├── LLMClient.java       # LLM客户端接口
│   │   ├── DeepSeekLLMClient.java
│   │   ├── OllamaLLMClient.java
│   │   ├── OpenAILLMClient.java
│   │   └── SimpleLLMClient.java
│   │
│   ├── memory/                   # 内存管理框架
│   │   ├── ReactMemory.java     # ReAct内存管理
│   │   └── Observation.java     # 观察结果
│   │
│   └── model/                    # 数据模型框架
│       ├── AgentRequest.java    # 智能体请求
│       ├── AgentResponse.java   # 智能体响应
│       └── ToolCall.java        # 工具调用
│
└── business/                     # ⭐ 业务代码（具体业务实现）
    │
    └── devops/                   # DevOps业务示例
        │
        ├── config/               # 业务配置
        │   └── LLMConfig.java   # LLM配置（DevOps业务专用）
        │
        ├── service/              # 业务服务
        │   ├── LLMClientService.java        # LLM客户端服务
        │   ├── AgentService.java            # 智能体服务（创建DevOps智能体）
        │   └── DevOpsOrchestrationService.java  # DevOps流程编排服务
        │
        └── Application.java      # DevOps业务启动类
```

---

## 🏗️ 框架代码（Framework）

### 位置
`src/main/java/framework/`

### 特点
- ✅ **可复用**：可以被多个业务模块使用
- ✅ **无业务逻辑**：不包含任何具体业务实现
- ✅ **稳定API**：提供清晰的接口供业务调用
- ⚠️ **不要修改**：业务开发者不应修改框架代码

### 核心组件

#### 1. Agent Framework (`framework/agent/`)
- `Agent` - 智能体接口
- `AgentFramework` - 智能体注册、路由、管理
- `ReActAgent` - ReAct循环实现

#### 2. LLM Client (`framework/llm/`)
- `LLMClient` - LLM客户端接口
- 各种LLM实现（DeepSeek、Ollama、OpenAI等）

#### 3. Memory Management (`framework/memory/`)
- `ReactMemory` - ReAct循环的内存管理
- `Observation` - 观察结果封装

#### 4. Data Models (`framework/model/`)
- `AgentRequest` - 智能体请求
- `AgentResponse` - 智能体响应
- `ToolCall` - 工具调用

---

## 💼 业务代码（Business）

### 位置
`src/main/java/business/`

### 特点
- ✅ **业务特定**：包含具体的业务逻辑
- ✅ **使用框架**：通过框架API实现业务
- ✅ **模块独立**：每个业务模块可以独立部署
- ✅ **易于扩展**：可以添加多个业务模块

### 业务模块结构

每个业务模块（如 `devops`）包含：

1. **config/** - 业务配置类
   - 业务相关的配置管理
   - 例如：`LLMConfig.java`

2. **service/** - 业务服务类
   - 业务逻辑实现
   - 使用框架API创建和管理智能体
   - 例如：`AgentService.java`, `OrchestrationService.java`

3. **Application.java** - 业务启动类
   - Spring Boot应用入口
   - 业务初始化逻辑

---

## 🔄 框架与业务的关系

```
┌─────────────────────────────────────┐
│       业务代码 (Business)            │
│  ┌───────────────────────────────┐  │
│  │  Application.java             │  │
│  │  ↓                            │  │
│  │  OrchestrationService         │  │
│  │  ↓                            │  │
│  │  AgentService                 │  │
│  └───────────────────────────────┘  │
│           ↓ 使用                      │
└─────────────────────────────────────┘
           ↓
┌─────────────────────────────────────┐
│      框架代码 (Framework)            │
│  ┌───────────────────────────────┐  │
│  │  AgentFramework               │  │
│  │  ReActAgent                   │  │
│  │  LLMClient                    │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

---

## 📝 开发规范

### ✅ 应该做的

1. **在business包中开发业务代码**
   ```java
   package business.yourbusiness.service;
   ```

2. **使用framework包的类**
   ```java
   import framework.agent.AgentFramework;
   import framework.agent.ReActAgent;
   ```

3. **通过框架API操作**
   ```java
   framework.registerAgent("name", agent);
   framework.chatWithMaster(request);
   ```

### ❌ 不应该做的

1. **不要修改framework代码**
   ```java
   // ❌ 错误
   // framework/agent/ReActAgent.java - 不要修改
   ```

2. **不要在framework中添加业务逻辑**
   ```java
   // ❌ 错误
   // framework/agent/DevOpsAgent.java - 不要创建
   ```

3. **不要硬编码业务配置到框架中**
   ```java
   // ❌ 错误
   // if (name.equals("devops_master")) { ... }
   ```

---

## 🚀 如何添加新业务

1. 在 `business/` 下创建新业务目录
2. 参考 `business/devops/` 的结构
3. 使用框架API实现业务逻辑
4. 详细步骤见 `BUSINESS_DEVELOPMENT_GUIDE.md`

---

## 📚 相关文档

- **业务开发指南**：`BUSINESS_DEVELOPMENT_GUIDE.md`
- **迁移指南**：`MIGRATION_GUIDE.md`
- **项目README**：`README.md`

